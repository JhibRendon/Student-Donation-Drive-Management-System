// server/route/donationRoute.js
// NOTE: This file has duplicate routes that are now handled in donorRoute.js
// Keeping this file for backward compatibility but routes are handled in donorRoute.js
import express from "express";

const router = express.Router();

// Routes moved to donorRoute.js to avoid conflicts
// All donation routes are now in /api/donor/donations

export default router;
